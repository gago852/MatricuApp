services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: matricuapp
      POSTGRES_USER: matricuapp
      POSTGRES_PASSWORD: matricuapp
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
